/* ANNOUMENT AND MARK */
export const id_car_mark_fn = {
  allowNull: false,
  defaultValue: 0,
  field: 'id_car_mark',
}

/* ANNOUMENT AND CarModel */
export const id_car_model_fn = {
  allowNull: false,
  defaultValue: 0,
  field: 'id_car_model',
}

/* ANNOUMENT AND Generation */
export const id_car_generation_fn = {
  allowNull: false,
  defaultValue: 0,
  field: 'id_car_generation',
}

/*USER & SESSION */
export const login_fn = {
  allowNull: false,
  field: 'login',
}
